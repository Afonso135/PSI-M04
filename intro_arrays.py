import numpy as np
números=np.array([10,14,-5,33,120])
 
for i in range(5):
    print(números[i])

for i in range(5):
    números[i]=0
    array_0d=np(40)

    array_0d=np.array(40)

    print(type(números))
    print(type(array_0d))
    print(array_0d)

    for x in números:
        print(x)

        for x in range(len(números)):
            print(números[x])

        #criar um array vazio
    vazio=np.empty(10)
    print(vazio)
    #criar um array com zero
    zeros=np.zeros(10)
    print(zeros)

 
