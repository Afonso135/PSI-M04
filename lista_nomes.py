"""Programa para ler um nome e mostrar o nome no formato:último,primeiro,segundo
"""
def Nomes()->str:
    nº_nomes=int(input("insira quantos nomes quer na lista"))
    nome=input("insira os nomes")
    nomes_separados=nome.split("")
    if len(nome)==1:
        return nome
    nome_alterado=nome_alterado[len(nome)-1]+", "

    len(nomes_separados)
    for i in nome[:len(nome)]:
        nome_alterado=nome_alterado + "n" +" "
        return nome_alterado.strip()



    
    